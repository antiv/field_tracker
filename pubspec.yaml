# Pub workspace root: one dependency resolution for the shared package and
# every app, so a core change compiles against all of them at once.
name: field_trackers_workspace
publish_to: none

environment:
  sdk: '>=3.10.0 <4.0.0'

workspace:
  - packages/tracker_core
  - apps/bird_tracker
  - apps/herp_tracker
